apiVersion: v1
kind: Service
metadata:
  name: {{ include "recipe-detection.fullname" . }}
  labels:
    {{- include "recipe-detection.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "recipe-detection.selectorLabels" . | nindent 4 }}
